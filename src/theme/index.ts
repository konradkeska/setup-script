import dark from "./theme_dark.json";
import light from "./theme_light.json";

export { dark, light };

export {
  Action,
  BrandColor,
  ActionColor,
  PrimaryColor,
  FontColor,
  MaterialColor,
  Padding,
  Shadow,
  Radius,
  ThemeMode,
} from "./types";

export type {
  IBrandColor,
  IColors,
  IFontColor,
  IMaterialColor,
  IPadding,
  IPrimaryColor,
  IRadius,
  IShadow,
} from "./types";
